
  <form>
    <% answers.forEach(function(answer) { %>
      <div class="radio">
        <label>
          <input type="radio" name="response[answer_id]" value="<%= answer.id %>">
          <%= answer.escape('text') %>
        </label>
      </div>
    <% }); %>
  </form>
